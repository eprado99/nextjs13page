import { DefaultConfig } from "@/types";

export const defaultConfig: DefaultConfig = {
    navBar: [
        {
            title: "Home",
            href: "/home"
        },
        {
            title: "Projects",
            href: "/project"
        }
    ]
}